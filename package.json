{
  "name": "daily-tools",
  "version": "1.0.0",
  "repository": "https://github.com/dorisentomari/daily-tools.git",
  "license": "MIT",
  "author": "dorisentomari <dorisentomari@gmail.com>",
  "scripts": {
    "git:pull": "git pull",
    "git:push": "git add . && git commit -m update && git push",
    "prettier": "prettier --write ./scripts/**",
    "push": "npm run git:pull && npm run prettier && npm run git:push"
  },
  "dependencies": {
    "moment": "^2.30.1"
  },
  "devDependencies": {
    "shelljs": "^0.8.5",
    "prettier": "^3.3.2"
  }
}
